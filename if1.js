//Solicite um número ao usuário.
// Use um bloco if para verificar se o número é par ou ímpar.
// Exiba o resultado.

const numero = 0;

if (numero % 2 === 0) {
    console.log(`Numero ${numero} é par `)
} else {
    console.log(`Numero ${numero} é impar`)
}