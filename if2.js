// Peça a idade do usuário.
// Utilize um bloco if para verificar se a pessoa é elegível para votar (idade maior ou igual a 18).
// Imprima uma mensagem indicando se ela pode ou não votar.

const idade = 12;

if (idade >= 18) {
    console.log (`Sua idade é ${idade}, já pode votar !`)
} else {
    console.log(`Sua idade é de ${idade}, não pode votar !`)
}